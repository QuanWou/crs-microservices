# CRS Frontend

Frontend của hệ thống đăng ký môn học, khởi tạo bằng Vite, React và TypeScript.

## Chạy development

```powershell
npm install
npm run dev
```

Frontend gọi API qua Gateway theo biến `VITE_API_BASE_URL` trong `.env`.
